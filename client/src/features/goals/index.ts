import { createGoal, deleteGoal, fetchGoals } from "./goalThunk";
import {
  selectGoalError,
  selectGoalLoading,
  selectGoals,
} from "./goalSelector";

export {
  createGoal,
  deleteGoal,
  fetchGoals,
  selectGoalError,
  selectGoalLoading,
  selectGoals,
};
